import React from 'react';

import styles from './style.module.scss';

export default function AboutPage() {
  return (
    <div className={styles.conatiner}>
      <ul className={styles.postListContainer}>{}</ul>
    </div>
  );
}
